# Server Configuration
PORT=4000
NODE_ENV=production

# ==========================================
# ⚠️  CÁC BIẾN DƯỚI ĐÂY PHẢI SET TRÊN RAILWAY DASHBOARD → Variables
# KHÔNG đặt DATABASE_URL ở đây — Railway tự inject từ PostgreSQL plugin
# ==========================================

# DATABASE_URL — Railway tự set nếu dùng PostgreSQL plugin
# DATABASE_URL="postgresql://user:pass@host:5432/dbname?sslmode=require"

# Internal Admin Key — bảo vệ /api/v1/admin/* và /api/v1/seed
# Đổi thành chuỗi ngẫu nhiên DÀI trước khi deploy lên production!
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
AFFILIATE_API_KEY="sk_aff_sktc_2026_secret_change_me"

# Future Security Settings (JWT etc.)
JWT_SECRET="changeme_in_production"
