# Server Configuration
PORT=4000
NODE_ENV=development

# Database Connection (SQLite Local)
DATABASE_URL="file:./dev.db"

# Future Security Settings (JWT etc.)
JWT_SECRET="changeme_in_production"

# Internal API Key — bắt buộc gửi trong header X-API-Key khi gọi /track/conversion
# Đổi thành chuỗi ngẫu nhiên dài trước khi lên production!
AFFILIATE_API_KEY="sk_aff_sktc_2026_secret_change_me"

# Backup: Supabase Connection (Uncomment and fill to switch back)
# DATABASE_URL="postgresql://postgres:[PASSWORD]@db.xxxx.supabase.co:6543/postgres"
